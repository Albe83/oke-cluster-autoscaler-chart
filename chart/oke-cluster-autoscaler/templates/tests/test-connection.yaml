apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "oke-cluster-autoscaler.fullname" . }}-test-connection"
  labels:
    {{- include "oke-cluster-autoscaler.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "oke-cluster-autoscaler.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
