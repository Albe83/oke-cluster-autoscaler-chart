apiVersion: v1
kind: Service
metadata:
  name: {{ include "oke-cluster-autoscaler.fullname" . }}
  labels:
    {{- include "oke-cluster-autoscaler.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "oke-cluster-autoscaler.selectorLabels" . | nindent 4 }}
